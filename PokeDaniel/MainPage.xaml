<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:PokeDaniel" x:Class="PokeDaniel.MainPage"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">
    <StackLayout VerticalOptions="Center">
        <StackLayout>
			<Button x:Name="ClickButton" Image="daniel.jpeg" Command="{Binding PokeCommand}" />
			<Label x:Name="TextLabel" Text="{Binding Text}" FontSize="Large" Margin="0,20,0,0" HorizontalTextAlignment="Center" />
            <Label x:Name="Text2Label" Text="{Binding Text2}" FontSize="Large" Margin="0,20,0,0" HorizontalTextAlignment="Center" />
        </StackLayout>
	</StackLayout>
</ContentPage>